name: "HCL AppScan SAST for AppScan 360 Instance"
on:
  workflow_dispatch: 
jobs:
  scan:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run AppScan SAST scan
        uses: HCL-TECH-SOFTWARE/appscan-sast-action@Issue_Fix_Main_Wait_For_Analysis_False
        with:
          asoc_key: ${{secrets.ASOC_KEY}}
          asoc_secret: ${{secrets.ASOC_SECRET}}
          application_id: ${{ secrets.ASOC_APP_ID }}
          static_analysis_only: true
          service_url: https://blmycldtl446131.nonprod.hclpnp.com
          acceptssl: true
          scan_name: "SAST ${{ steps.date.outputs.date }} Incremental scan"
          personal_scan: false
          wait_for_analysis: true
          incremental_scan: false
          fail_for_noncompliance: false
          failure_threshold: High
